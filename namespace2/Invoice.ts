namespace Runoob {
  export namespace invoiceApp {
    export class Invoice {
      /**
       * calculateDiscount
       */
      public calculateDiscount(price: number) {
        return price * 0.4;
      }
    }
  }
}
